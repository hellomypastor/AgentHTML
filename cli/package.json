{
  "name": "agenthtml",
  "version": "0.1.0",
  "description": "CLI for AgentHTML — init, preview, and validate HTML artifacts",
  "license": "Apache-2.0",
  "bin": {
    "agenthtml": "./dist/index.js"
  },
  "files": [
    "dist/",
    "skills/"
  ],
  "scripts": {
    "build": "node scripts/bundle-skills.mjs && tsc",
    "prepublishOnly": "npm run build"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
